type User {
  id: Int
  name: String
}

type Issue {
  id: Int
  title: String!
  content: String!
  option_list_json: String
  c_time: DateTime
  m_time: DateTime
}

type Query {
  users: [User]
}

type Mutation {
  createIssue(title: String, content: String, tag_id: Int): Issue!
}


scalar DateTime
